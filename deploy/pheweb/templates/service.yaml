apiVersion: v1
kind: Service
metadata:
  name: {{ include "pheweb.fullname" . }}
  labels:
    {{- include "pheweb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pheweb.selectorLabels" . | nindent 4 }}
