/* eslint-env jest */

test('1', () => {
  /*
  const fail = () => { throw (new TypeError()) }

  const gene = '22f0c65c'
  const phenotype = 'd4ff3eea'
  expect(() => createParameter('', fail)).toThrow(TypeError)
  expect(() => createParameter('/gene', fail)).toThrow(TypeError)
  expect(() => createParameter(`/gene/${gene}`, fail)).toThrow(TypeError)
  expect(createParameter(`/gene/${gene}`, fail)).toStrictEqual({ phenotype: undefined, gene: gene })
  expect(() => createParameter(`/gene/${gene}/pheno`, fail)).toThrow(TypeError)
  expect(createParameter(`/gene/${gene}/pheno/${phenotype}`, fail)).toStrictEqual({ phenotype: phenotype, gene: gene })

   */
})
